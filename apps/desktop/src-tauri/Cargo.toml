[package]
name = "ai-novel-factory-desktop"
version = "0.1.0"
description = "Tauri desktop shell for the AI Novel Factory shared frontend"
authors = ["AI Novel Factory Contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
